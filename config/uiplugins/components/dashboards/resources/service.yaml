apiVersion: v1
kind: Service
metadata:
  name: dashboards
  labels:
    app.kubernetes.io/instance: dashboards
    app.kubernetes.io/managed-by: observability-operator
    app.kubernetes.io/part-of: UIPlugin
  annotations:
    service.alpha.openshift.io/serving-cert-secret-name: dashboards
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/instance: dashboards
    app.kubernetes.io/managed-by: observability-operator
    app.kubernetes.io/part-of: UIPlugin
  ports:
    - name: http
      port: 9443
      protocol: TCP
      targetPort: 9443
