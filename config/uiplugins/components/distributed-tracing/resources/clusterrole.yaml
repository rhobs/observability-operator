apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: distributed-tracing
rules:
  - apiGroups:
      - tempo.grafana.com
    resources:
      - tempostacks
      - tempomonolithics
    verbs:
      - list
